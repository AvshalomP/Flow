import React from 'react';
import { Container } from './styles';
import Header from './components/Header';

const Home = (props) => {
  return (
    <Container>
      <Header />
    </Container>
  );
};

export default Home;
