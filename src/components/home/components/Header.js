import React from 'react';
import { HeaderContainer, HomeBtn, ThemeBtn } from './styles';

const Header = (props) => {
  return (
    <HeaderContainer>
      <HomeBtn>F_</HomeBtn>
      <ThemeBtn>Tgl Theme</ThemeBtn>
    </HeaderContainer>
  );
};

export default Header;
