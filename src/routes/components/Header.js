import React from 'react';
import { HeaderContainer, HomeBtn, ThemeBtn } from './styles';

const Header = (props) => {
  return (
    <HeaderContainer>
      <HomeBtn>F_</HomeBtn>
      <ThemeBtn />
    </HeaderContainer>
  );
};

export default Header;
